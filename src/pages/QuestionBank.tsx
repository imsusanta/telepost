import { useState, useEffect, useCallback, useRef } from "react";
import { Database, RefreshCw, Search, Trash2, FileText, List, Zap, Download, Pencil, ChevronLeft, ChevronRight, ArrowDownAz, ArrowUpAz, Globe, Lock } from "lucide-react";
import DashboardLayout from "@/components/DashboardLayout";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useToast } from "@/hooks/use-toast";
import { QuestionBankService, QuestionBankItem, QuestionBankFilters, QuestionBankStatistics } from "@/services/questionBankService";
import { ClassificationMetadataService } from "@/services/classificationMetadataService";
import { useSubscription } from "@/hooks/useSubscription";
import { supabase } from "@/integrations/supabase/client";
import { Skeleton } from "@/components/ui/skeleton";
import { AddQuestionDialog } from "@/components/AddQuestionDialog";
import { AIQuestionGenerator } from "@/components/AIQuestionGenerator";
import { QuestionSelectionDialog } from "@/components/QuestionSelectionDialog";
import { Checkbox } from "@/components/ui/checkbox";
import { TelegramShareQuestionBank } from "@/components/TelegramShareQuestionBank";
import { BulkUploadDialog } from "@/components/BulkUploadDialog";
import { ParsedQuestion } from "@/utils/questionParser";
import { EditQuestionDialog } from "@/components/EditQuestionDialog";
import { ClassificationBadges } from "@/components/ClassificationBadges";
import { QuestionFilters } from "@/components/QuestionFilters";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog";

export default function QuestionBank() {
  const [questions, setQuestions] = useState<QuestionBankItem[]>([]); const [loading, setLoading] = useState(true); const [isRefreshing, setIsRefreshing] = useState(false); const [statsLoading, setStatsLoading] = useState(false); const [currentUserId, setCurrentUserId] = useState<string | null>(null); const [searchQuery, setSearchQuery] = useState(""); const [filters, setFilters] = useState<QuestionBankFilters>({ includePublic: true }); const [stats, setStats] = useState<QuestionBankStatistics | null>(null); const [currentPage, setCurrentPage] = useState(1); const [totalCount, setTotalCount] = useState(0); const [sortOrder, setSortOrder] = useState<"asc" | "desc">("asc"); const [pageSize, setPageSize] = useState(20); const searchTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const [generatedQuestions, setGeneratedQuestions] = useState<Array<{ question: string; options: string[]; correct_option_index: number; explanation?: string }>>([]); const [showSelectionDialog, setShowSelectionDialog] = useState(false); const [defaultTopic, setDefaultTopic] = useState(""); const [defaultLanguage, setDefaultLanguage] = useState("en"); const [selectedQuestionIds, setSelectedQuestionIds] = useState<Set<string>>(new Set()); const [editingQuestion, setEditingQuestion] = useState<QuestionBankItem | null>(null); const [isEditDialogOpen, setIsEditDialogOpen] = useState(false); const [deleteQuestionId, setDeleteQuestionId] = useState<string | null>(null); const [showBulkDeleteDialog, setShowBulkDeleteDialog] = useState(false); const [subjectsWithCounts, setSubjectsWithCounts] = useState<{ subject: string; count: number }[]>([]); const [fullSubjects, setFullSubjects] = useState<any[]>([]); const [fullTopics, setFullTopics] = useState<any[]>([]); const [topicsWithCounts, setTopicsWithCounts] = useState<{ topic: string; count: number }[]>([]);
  const { toast } = useToast(); const { canAccess } = useSubscription(); const hasAIAccess = canAccess("question_bank", "ai_generate");
  const applyStatistics = useCallback((s: QuestionBankStatistics) => { setStats(s); setTopicsWithCounts(Object.entries(s.byTopic).map(([topic, count]) => ({ topic, count })).sort((a,b)=>b.count-a.count)); setSubjectsWithCounts(Object.entries(s.bySubject).map(([subject,count])=>({subject,count})).sort((a,b)=>b.count-a.count)); }, []);
  const loadQuestions = useCallback(async (page = currentPage, query = searchQuery) => { try { const { data:{ user } } = await supabase.auth.getUser(); if (!user) return; setCurrentUserId(user.id); const offset=(page-1)*pageSize; const { data,count }=await QuestionBankService.getQuestions(user.id,filters,pageSize,offset,query,sortOrder); setQuestions(data); setTotalCount(count??0); } catch(error:unknown){toast({title:"Error",description:error instanceof Error?error.message:"Failed to load questions",variant:"destructive"});} finally{setLoading(false);} },[filters,pageSize,sortOrder,currentPage,searchQuery,toast]);
  const loadStats = useCallback(async()=>{setStatsLoading(true);try{const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Authentication required");applyStatistics(await QuestionBankService.getStatistics(user.id,true));}catch(error:unknown){setStats(null);toast({title:"Stats unavailable",description:error instanceof Error?error.message:"Failed to load statistics",variant:"destructive"});}finally{setStatsLoading(false);}},[applyStatistics,toast]);
  const loadClassificationData=useCallback(async()=>{const [s,t]=await Promise.allSettled([ClassificationMetadataService.getSubjects(),ClassificationMetadataService.getAllTopics()]);setFullSubjects(s.status==="fulfilled"?s.value:[]);setFullTopics(t.status==="fulfilled"?t.value:[]);},[]);
  const refreshAll=useCallback(async(showToast=false)=>{setIsRefreshing(true);try{await Promise.all([loadQuestions(currentPage,searchQuery),loadStats(),loadClassificationData()]);if(showToast)toast({title:"Refreshed",description:"Question Bank updated."});}finally{setIsRefreshing(false);}},[currentPage,searchQuery,loadQuestions,loadStats,loadClassificationData,toast]);
  useEffect(()=>{loadQuestions(currentPage,searchQuery);loadStats();loadClassificationData();},[currentPage,filters,pageSize,sortOrder]);
  useEffect(()=>{if(searchTimeoutRef.current)clearTimeout(searchTimeoutRef.current);searchTimeoutRef.current=setTimeout(()=>{setCurrentPage(1);loadQuestions(1,searchQuery);},500);return()=>{if(searchTimeoutRef.current)clearTimeout(searchTimeoutRef.current);};},[searchQuery]);
  const handleRefresh=()=>refreshAll(true);
  const handleAddSubject=async(name:string)=>{if(!currentUserId)return;try{await ClassificationMetadataService.createSubject(name,currentUserId);await refreshAll();toast({title:"Success",description:`Subject "${name}" created successfully`});}catch(error:any){toast({title:"Error",description:error.message||"Failed to create subject",variant:"destructive"});}};
  const handleEditSubject=async(oldName:string,newName:string)=>{try{const subject=fullSubjects.find(s=>s.name===oldName);if(subject)await ClassificationMetadataService.updateSubject(subject.id,{name:newName});const {data:{user}}=await supabase.auth.getUser();if(user)await supabase.from("question_banks").update({subject:newName} as any).eq("user_id",user.id).eq("subject",oldName);await refreshAll();}catch(error:any){toast({title:"Error",description:error.message||"Failed to edit subject",variant:"destructive"});}};
  const handleDeleteSubject=async(name:string)=>{try{const subject=fullSubjects.find(s=>s.name===name);if(subject)await ClassificationMetadataService.deleteSubject(subject.id);const {data:{user}}=await supabase.auth.getUser();if(user)await supabase.from("question_banks").update({subject:""} as any).eq("user_id",user.id).eq("subject",name);await refreshAll();}catch(error:any){toast({title:"Error",description:error.message||"Failed to delete subject",variant:"destructive"});}};
  const handleEditTopic=async(oldName:string,newName:string)=>{try{const topic=fullTopics.find(t=>t.name===oldName);if(topic)await ClassificationMetadataService.updateTopic(topic.id,newName);const {data:{user}}=await supabase.auth.getUser();if(user)await supabase.from("question_banks").update({topic:newName} as any).eq("user_id",user.id).eq("topic",oldName);await refreshAll();}catch(error:any){toast({title:"Error",description:error.message||"Failed to edit topic",variant:"destructive"});}};
  const handleDeleteTopic=async(name:string)=>{try{const topic=fullTopics.find(t=>t.name===name);if(topic)await ClassificationMetadataService.deleteTopic(topic.id);const {data:{user}}=await supabase.auth.getUser();if(user)await supabase.from("question_banks").update({topic:""} as any).eq("user_id",user.id).eq("topic",name);await refreshAll();}catch(error:any){toast({title:"Error",description:error.message||"Failed to delete topic",variant:"destructive"});}};
  const handleAddTopic=async(subjectId:string,name:string)=>{if(!currentUserId)return;try{await ClassificationMetadataService.createTopic(subjectId,name,currentUserId);await refreshAll();toast({title:"Success",description:`Topic "${name}" created successfully`});}catch(error:any){toast({title:"Error",description:error.message||"Failed to create topic",variant:"destructive"});}};
  const handleQuestionsGenerated=(qs:Array<{question:string;options:string[];correct_option_index:number;explanation?:string}>,topic?:string,language?:string)=>{setGeneratedQuestions(qs);setDefaultTopic(topic||"");setDefaultLanguage(language||"en");setShowSelectionDialog(true);};
  const handleQuestionsSaved=async()=>{setGeneratedQuestions([]);setShowSelectionDialog(false);await refreshAll();};
  const handleBulkUpload=async(qs:ParsedQuestion[])=>{try{const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("You must be logged in to upload questions.");await QuestionBankService.bulkAddQuestions(user.id,qs.map(q=>({question:q.question,options:q.options,correct_option_index:q.correct_option_index,explanation:q.explanation||undefined,subject:q.subject||"GK",topic:q.topic||"",language:"bn",is_public:false,is_active:true})));await refreshAll();toast({title:"Success",description:`Successfully uploaded ${qs.length} questions.`});}catch(error:any){toast({title:"Upload Failed",description:error.message||"An error occurred",variant:"destructive"});throw error;}};
  const handleToggleQuestion=(id:string)=>{const next=new Set(selectedQuestionIds);if(next.has(id))next.delete(id);else next.add(id);setSelectedQuestionIds(next);};
  const handleClearSelection=()=>setSelectedQuestionIds(new Set());
  const handleBulkDelete=async()=>{if(!selectedQuestionIds.size)return;try{const {data:{user}}=await supabase.auth.getUser();if(!user)return;for(const id of selectedQuestionIds)await QuestionBankService.deleteQuestion(id,user.id);const count=selectedQuestionIds.size;setSelectedQuestionIds(new Set());await refreshAll();toast({title:"Bulk Delete Complete",description:`Successfully deleted ${count} questions.`});}catch(error:unknown){toast({title:"Error",description:error instanceof Error?error.message:"Failed to delete some questions",variant:"destructive"});}};
  const handleEdit=(q:QuestionBankItem)=>{setEditingQuestion(q);setIsEditDialogOpen(true);};
  const confirmDelete=async()=>{if(!deleteQuestionId)return;try{const {data:{user}}=await supabase.auth.getUser();if(!user)return;await QuestionBankService.deleteQuestion(deleteQuestionId,user.id);setDeleteQuestionId(null);await refreshAll();toast({title:"Deleted",description:"Question deleted successfully"});}catch(error:unknown){toast({title:"Error",description:error instanceof Error?error.message:"Failed to delete question",variant:"destructive"});}};
  const handleExportQuestions=()=>{if(!questions.length)return toast({title:"No questions to export",variant:"destructive"});let text=`Question Bank Export\nTotal Questions: ${questions.length}\n${"=".repeat(50)}\n\n`;questions.forEach((q,i)=>{text+=`${i+1}. ${q.question}\n`;q.options.forEach((o,j)=>text+=`   ${String.fromCharCode(97+j)}) ${o}\n`);text+=`   Correct Answer: ${String.fromCharCode(97+q.correct_option_index)}) ${q.options[q.correct_option_index]}\n`;if(q.explanation)text+=`   Explanation: ${q.explanation}\n`;text+="\n";});const url=URL.createObjectURL(new Blob([text],{type:"text/plain;charset=utf-8"}));const a=document.createElement("a");a.href=url;a.download=`question_bank_export_${new Date().toISOString().slice(0,10)}.txt`;a.click();URL.revokeObjectURL(url);};
  const handleFiltersChange=(newFilters:QuestionBankFilters)=>{setCurrentPage(1);setFilters(newFilters);};
  return <DashboardLayout><div className="space-y-6"><div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4"><div><h1 className="text-2xl md:text-4xl font-bold flex items-center gap-2"><Database className="w-6 h-6 md:w-10 md:h-10" />Question Bank</h1><p className="text-sm text-muted-foreground">Total: <span className="text-foreground font-bold">{statsLoading?"—":stats?.total??0}</span></p></div><div className="flex gap-2 flex-wrap"><Button variant="outline" size="sm" onClick={()=>setSortOrder(v=>v==="desc"?"asc":"desc")}><ArrowDownAz className="w-4 h-4" />{sortOrder==="desc"?"Newest":"Oldest"}</Button><Button variant="outline" size="sm" onClick={handleExportQuestions}><Download className="w-4 h-4" />Export</Button><Button variant="outline" size="sm" onClick={handleRefresh} disabled={isRefreshing}><RefreshCw className={`w-4 h-4 ${isRefreshing?"animate-spin":""}`} />Refresh</Button></div></div><Card><CardHeader><CardTitle>Question Bank Statistics</CardTitle></CardHeader><CardContent><div className="grid grid-cols-2 md:grid-cols-5 gap-3"><div className="rounded-lg border p-3"><div className="text-xs text-muted-foreground">Total</div><div className="text-2xl font-bold">{stats?.total??0}</div></div><div className="rounded-lg border p-3"><div className="text-xs text-muted-foreground">Subjects</div><div className="text-2xl font-bold">{subjectsWithCounts.length}</div></div><div className="rounded-lg border p-3"><div className="text-xs text-muted-foreground">Topics</div><div className="text-2xl font-bold">{topicsWithCounts.length}</div></div><div className="rounded-lg border p-3"><div className="text-xs text-muted-foreground">Public</div><div className="text-2xl font-bold">{stats?.publicCount??0}</div></div><div className="rounded-lg border p-3"><div className="text-xs text-muted-foreground">Private</div><div className="text-2xl font-bold">{stats?.privateCount??0}</div></div></div></CardContent></Card><Tabs defaultValue="questions"><TabsList className={`grid w-full ${hasAIAccess?"grid-cols-2":"grid-cols-1"}`}><TabsTrigger value="questions"><List className="w-4 h-4" />My Questions</TabsTrigger>{hasAIAccess&&<TabsTrigger value="ai-generate"><Zap className="w-4 h-4" />AI Generate</TabsTrigger>}</TabsList><TabsContent value="questions" className="space-y-6 mt-6"><div className="flex flex-col sm:flex-row gap-2"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" /><Input placeholder="Search questions..." value={searchQuery} onChange={(e)=>setSearchQuery(e.target.value)} className="pl-10" /></div><div className="flex gap-2"><BulkUploadDialog onUpload={handleBulkUpload} fullSubjects={fullSubjects} fullTopics={fullTopics} currentCount={stats?.total||0} /><AddQuestionDialog onQuestionAdded={handleRefresh} currentCount={stats?.total||0} /><TelegramShareQuestionBank selectedQuestionIds={selectedQuestionIds} onClearSelection={handleClearSelection} /></div></div><QuestionFilters filters={filters} onFiltersChange={handleFiltersChange} subjectsWithCounts={subjectsWithCounts} topicsWithCounts={topicsWithCounts} fullSubjects={fullSubjects} fullTopics={fullTopics} subjectTopics={stats?.subjectTopics} totalCount={stats?.total||0} filteredCount={totalCount} onAddSubject={handleAddSubject} onEditSubject={handleEditSubject} onDeleteSubject={handleDeleteSubject} onAddTopic={handleAddTopic} onEditTopic={handleEditTopic} onDeleteTopic={handleDeleteTopic} privateOnly={false}/>{selectedQuestionIds.size>0&&<div className="flex items-center justify-between p-3 bg-primary/5 border rounded-lg"><span className="font-bold text-primary">{selectedQuestionIds.size} selected</span><Button onClick={()=>setShowBulkDeleteDialog(true)} variant="destructive"><Trash2 className="w-4 h-4 mr-2" />Delete Selected</Button></div>}{loading?<div className="space-y-3">{Array.from({length:5}).map((_,i)=><Skeleton key={i} className="h-24 w-full"/>)}</div>:questions.length===0?<Card><CardContent className="py-10 text-center text-muted-foreground">No questions found.</CardContent></Card>:<div className="space-y-3">{questions.map((q,idx)=><Card key={q.id}><CardContent className="p-4"><div className="flex items-start gap-3"><Checkbox checked={selectedQuestionIds.has(q.id)} onCheckedChange={()=>handleToggleQuestion(q.id)}/><div className="flex-1 min-w-0"><div className="font-semibold">{(currentPage-1)*pageSize+idx+1}. {q.question}</div><div className="grid grid-cols-1 md:grid-cols-2 gap-1 mt-2 text-sm">{q.options.map((o,i)=><div key={i} className={i===q.correct_option_index?"font-semibold":""}>{String.fromCharCode(65+i)}. {o}{i===q.correct_option_index?" ✓":""}</div>)}</div><div className="flex flex-wrap gap-2 mt-2"><ClassificationBadges subject={q.subject} topic={q.topic}/><span className="text-xs text-muted-foreground">{q.language}</span>{q.is_public?<Globe className="w-4 h-4"/>:<Lock className="w-4 h-4"/>}</div></div><div className="flex gap-1"><Button variant="ghost" size="icon" onClick={()=>handleEdit(q)}><Pencil className="w-4 h-4"/></Button><Button variant="ghost" size="icon" onClick={()=>setDeleteQuestionId(q.id)}><Trash2 className="w-4 h-4"/></Button></div></div></CardContent></Card>)}</div>}<div className="flex items-center justify-between border-t pt-4"><span className="text-sm text-muted-foreground">Showing {totalCount===0?0:(currentPage-1)*pageSize+1}-{Math.min(currentPage*pageSize,totalCount)} of {totalCount}</span><div className="flex items-center gap-2"><Select value={String(pageSize)} onValueChange={v=>{setPageSize(Number(v));setCurrentPage(1);}}><SelectTrigger className="w-20"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="20">20</SelectItem><SelectItem value="50">50</SelectItem><SelectItem value="100">100</SelectItem></SelectContent></Select><Button variant="outline" size="icon" onClick={()=>setCurrentPage(v=>Math.max(1,v-1))} disabled={currentPage===1}><ChevronLeft className="w-4 h-4"/></Button><span className="text-sm">Page {currentPage}</span><Button variant="outline" size="icon" onClick={()=>setCurrentPage(v=>v+1)} disabled={currentPage*pageSize>=totalCount}><ChevronRight className="w-4 h-4"/></Button></div></div></TabsContent><TabsContent value="ai-generate" className="mt-6"><AIQuestionGenerator onQuestionsGenerated={handleQuestionsGenerated}/></TabsContent></Tabs><AlertDialog open={!!deleteQuestionId} onOpenChange={open=>!open&&setDeleteQuestionId(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Delete question?</AlertDialogTitle><AlertDialogDescription>This action cannot be undone.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={confirmDelete}>Delete</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><AlertDialog open={showBulkDeleteDialog} onOpenChange={setShowBulkDeleteDialog}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Delete selected questions?</AlertDialogTitle><AlertDialogDescription>This action cannot be undone.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={async()=>{setShowBulkDeleteDialog(false);await handleBulkDelete();}}>Delete</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><QuestionSelectionDialog open={showSelectionDialog} onOpenChange={setShowSelectionDialog} questions={generatedQuestions} onSaved={handleQuestionsSaved} defaultTopic={defaultTopic} defaultLanguage={defaultLanguage}/>{editingQuestion&&<EditQuestionDialog open={isEditDialogOpen} onOpenChange={setIsEditDialogOpen} question={editingQuestion} onSaved={handleRefresh}/>}</div></DashboardLayout>;
}
